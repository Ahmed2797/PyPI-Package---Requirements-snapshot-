Project: Pipy Snapshot
Version: 0.1.0
Author: Ahmed2797

Description:
A Python package for generating requirements lock files with multiple formats.

Features:
1. Generate lock files from requirements.txt
2. Multiple output formats (JSON, YAML, TOML, traditional)
3. Simple CLI interface
4. Package validation

Usage:
$ pip install pipy-snapshot
$ pipy lock -f requirements.txt --format json
$ pipy check -f requirements.txt

Project Structure:
- pipy/ : Main package
- app.py : Application entry point
- setup.py : Package setup
- requirements.txt : Dependencies

To Build:
$ python -m build
$ twine upload dist/*

To Install Locally:
$ pip install -e .

---------------------------------
python app.py check -f requirements.txt
python app.py validate -f requirements.txt
python app.py lock -f requirements.txt --format json
python app.py lock -f requirements.txt --format yaml
python app.py lock -f requirements.txt --format toml
python app.py diff -f requirements.txt



pip install -e .

# ১. Version চেক করুন
reqsnap --version

# ২. Help দেখুন
reqsnap --help

# ৩. Lock command টেস্ট করুন
reqsnap lock -f requirements.txt

# ৪. Check command টেস্ট করুন
reqsnap check -f requirements.txt

# ৫. Validate command টেস্ট করুন
reqsnap validate -f requirements.txt


# README.md আপডেট করুন

## 🚀 Installation

```bash
pip install reqsnap

# Generate requirements.lock from requirements.txt
reqsnap lock

# Use different requirements file
reqsnap lock --file requirements-dev.txt

# Output in different formats
reqsnap lock --format json
reqsnap lock --format yaml
reqsnap lock --format toml



## **এখন পাবলিশ করুন!**

```bash
echo "🎯 Ready to publish to PyPI!"
echo ""
echo "Step 1: TestPyPI (for testing)"
echo "------------------------------"
echo "twine upload --repository testpypi dist/*"
echo ""
echo "Step 2: Test installation from TestPyPI"
echo "--------------------------------------"
echo "# In new terminal:"
echo "pip install --index-url https://test.pypi.org/simple/ reqsnap"
echo ""
echo "Step 3: Real PyPI"
echo "-----------------"
echo "twine upload dist/*"
echo ""
echo "Step 4: Verify"
echo "-------------"
echo "pip install reqsnap"
echo "reqsnap --version"